# news-retrieval-agent

Knowledge retrieval and question answering over a news corpus.

The system builds a retrieval layer, exposes it as tools, and uses an LLM agent to answer questions with grounded citations — or refuse when the corpus does not support an answer.
